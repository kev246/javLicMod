package App.LicenseClient;
import Liscense.encDecrExample;
import com.sun.javafx.scene.control.skin.TableHeaderRow;

import java.io.*;
import java.security.GeneralSecurityException;
import java.util.Properties;

public class mainRunner {

    public static void main(String[] args) throws IOException, GeneralSecurityException {
        /*String VALID_KEY = "0070123875";
        final String EncFile = "encrypted.properties";
        final String KEY_FILE = "EncKeyFile.key";
        Properties PEncFileLoader = new Properties();
        licenseCheck lc = new licenseCheck();
        System.out.println("id from info file after checking is : "+lc.checkLicense(lc.LiPath).getId());*/

/*
 ToDo activation is only valid if signature is valid for PN and if PN encryption cypher is same as PMs 16 digit encryption data since we
    creating Pn from Pm's key now Pn is overwritten on top of PD (check will be
 TODO   if Pd ==pm (here Pd is replaced with Pn (signed) then CHECK IF SIGNATURE ALSO VALID OR NOT full liscence and full version , START APP FULL
            ELSE Pd != Pm and Pd not signed, and Pd value is known value, then display demo lis , please request for activation for full AND START LIGHT
            VERSION OF APP.
*/
    }

}
